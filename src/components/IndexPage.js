import React from 'react';
import Game from './Game';
// import PropTypes from 'prop-types';

const Index = (props) => {
  return (
    <Game />
  );
};

Index.propTypes = {
};

export default Index;
